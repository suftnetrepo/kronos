node_modules/
.expo/
dist/
*.orig.*
web-build/
.DS_Store

# Native projects are generated on demand via `expo prebuild` — not committed.
ios/
android/

# Downloaded EAS build artifacts
*.aab
*.apk
*.ipa

# Android release signing keystore + passwords — never commit. Back this
# up somewhere durable (password manager / secure cloud storage): losing
# it means losing the ability to publish updates to this app on Play.
credentials/

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli*.zip
